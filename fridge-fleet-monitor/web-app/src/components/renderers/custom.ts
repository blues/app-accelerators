/**
 * This file is imported and allows registration of custom renderings for new sensors.
 *
 */

import * as Renderers from "./registry";

export function registerRenderers(
  rendererRegistry: Renderers.RendererRegistry
) {}
